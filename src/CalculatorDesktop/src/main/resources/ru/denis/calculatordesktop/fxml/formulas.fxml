<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.*?>
<VBox xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="ru.denis.calculatordesktop.controller.FormulasController">

    <TabPane fx:id="tabPane" tabClosingPolicy="UNAVAILABLE" VBox.vgrow="ALWAYS">

        <!-- ── Formulas tab ── -->
        <Tab text="Формулы">
            <VBox spacing="0">
                <HBox styleClass="page-header" alignment="CENTER_LEFT" spacing="12">
                    <Label text="Формулы" styleClass="page-title"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <ComboBox fx:id="groupFilter" promptText="Все группы"
                              prefWidth="180" onAction="#handleGroupFilter"/>
                    <TextField fx:id="searchField" promptText="Поиск..."
                               styleClass="search-field" prefWidth="200"
                               onKeyReleased="#handleSearch"/>
                    <Button text="+ Добавить" styleClass="btn-primary" onAction="#handleAdd"/>
                </HBox>
                <VBox styleClass="page-body" VBox.vgrow="ALWAYS">
                    <TableView fx:id="formulasTable" VBox.vgrow="ALWAYS">
                        <columns>
                            <TableColumn fx:id="colName"       text="Название"    prefWidth="220"/>
                            <TableColumn fx:id="colExpression" text="Выражение"   prefWidth="280"/>
                            <TableColumn fx:id="colGroup"      text="Группа"      prefWidth="160"/>
                            <TableColumn fx:id="colActions"    text=""            prefWidth="100"/>
                        </columns>
                        <placeholder><Label text="Нет формул"/></placeholder>
                    </TableView>
                </VBox>
            </VBox>
        </Tab>

        <!-- ── Groups tab ── -->
        <Tab text="Группы формул">
            <VBox spacing="0">
                <HBox styleClass="page-header" alignment="CENTER_LEFT" spacing="12">
                    <Label text="Группы формул" styleClass="page-title"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button text="+ Добавить группу" styleClass="btn-primary"
                            onAction="#handleAddGroup"/>
                </HBox>
                <VBox styleClass="page-body" VBox.vgrow="ALWAYS">
                    <TableView fx:id="groupsTable" VBox.vgrow="ALWAYS">
                        <columns>
                            <TableColumn fx:id="colGroupName"    text="Название" prefWidth="360"/>
                            <TableColumn fx:id="colGroupActions" text=""         prefWidth="120"/>
                        </columns>
                        <placeholder><Label text="Нет групп"/></placeholder>
                    </TableView>
                </VBox>
            </VBox>
        </Tab>

    </TabPane>
</VBox>
